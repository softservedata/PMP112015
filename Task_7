#include "iostream" 
using namespace std; 

int main() 
{ 
int a; 
cout << " Enter a : "; 
cin >> a; 
for (int i = 1; i <= a; ++i) 
{ 
if ((i % 3 == 0) && (!(i % 5 == 0))) 
cout << i << endl; 


} 
system("pause"); 
return 0; 
}
